# dirk_dashboard
The simple dashboard for Dirk to monitor the tasks

## Dependencies:
nodejs v8.10.0
sqlite3 3.22.0

## Deployment:

Install lib dependencies:

```sh
npm install
```


Migrate DB:

```sh
sequelize db:migrate
```


Start the server:

```sh
npm start
`
```
